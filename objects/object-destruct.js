const student = {
  name: "Ram",
  city: "Vijaywada",
  state: "Andhra Pradesh"
};

// p1 is the student.name, p2 is "address" object
const { name, city, state } = student;

console.log(name);
console.log(city);
console.log(state);
